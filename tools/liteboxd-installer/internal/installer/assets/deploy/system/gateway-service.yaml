apiVersion: v1
kind: Service
metadata:
  name: liteboxd-gateway
spec:
  selector:
    app: liteboxd-gateway
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
  type: ClusterIP
